---
title: "Report"
format: md
message: false
---

## Iris

Iris table

```{r}
#| message: false
library(tidyverse)
count(iris, Species)
```

table

```{r}
library(knitr)
kable(count(iris, Species))
```

```{r}
library(gt)
gt(count(iris, Species))
```

ggplot
```{r}
ggplot(iris, aes(Species, Sepal.Length)) +
  geom_boxplot()
```
